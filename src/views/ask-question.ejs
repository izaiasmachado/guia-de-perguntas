<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>

    <title>Realizar pergunta</title>
  </head>
  <body class="container-fluid p-0 m-0 h-100">
    <div class="d-flex flex-column">
      <%- include('partials/navbar') %>

      <main class="container col-11 col-md-8 mt-3 mb-3">
        <h3>Realizar Pergunta</h3>
        <hr />
        <form action="/ask" method="POST">
          <label for="title" class="form-label">Título</label>
          <input
            type="text"
            name="title"
            class="form-control <%= errors.title ? 'is-invalid' : '' %>"
            value="<%= data?.title %>"
            aria-describedby="validationServerSideTitle"
          />

          <div id="validationServerSideTitle" class="invalid-feedback">
            <%= errors.title?._errors?.[0] %>
          </div>

          <label for="content" class="form-label">Descrição</label>
          <textarea
            name="content"
            class="form-control <%= errors?.content ? 'is-invalid' : '' %>"
            aria-describedby="validationServerSideContent"
          ></textarea>

          <div id="validationServerSideContent" class="invalid-feedback">
            <%= errors.content?._errors?.[0] %>
          </div>

          <button type="submit" class="btn btn-primary mt-3">
            Enviar pergunta
          </button>
        </form>
      </main>
    </div>
  </body>
</html>
